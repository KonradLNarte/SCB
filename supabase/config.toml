# Supabase Configuration for SCB API Integration

[api]
# Port to use for the API URL
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API endpoints.
schemas = ["public", "storage", "graphql_public"]
# Extra schemas to add to the search_path of every request.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure.
max_rows = 1000

[db]
# Port to use for the local database URL
port = 54322
# The database major version to use.
major_version = 15

[studio]
# Port to use for Supabase Studio
port = 54323

# Edge Functions configuration
[functions.scb-api-test]
# This section is for the scb-api-test Edge Function
verify_jwt = false  # Disable JWT verification for testing
